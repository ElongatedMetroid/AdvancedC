#include <stdio.h>

int main(void)
{
    int sum, data[100];
    int i = (5, 10);        //10 is assigned to i
    //int j = (f1(), f2());   //f1 is going to be called an evaluated first than f2

    while(i < 100)
        sum+=data[i], ++i;   

    return 0;
}